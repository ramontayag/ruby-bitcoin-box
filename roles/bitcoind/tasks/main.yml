---
- name: "Copy install script for bitcoind"
  template: src=install_bitcoind.rb.j2 dest={{home_dir}}/install_bitcoind.rb

- name: "Download and install bitcoind v0.7.2"
  shell: ruby install_bitcoind.rb creates={{home_dir}}/bitcoin-0.7.2-linux
